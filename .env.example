# --- Local development ---
# Copy to .env.local. .env.local is gitignored.

# Local Ollama (default): run `ollama serve` and e.g. `ollama pull phi3`.
# Leave VITE_OLLAMA_USE_CLOUD unset. No .env needed; Vite proxies /api/ollama-local.

# Ollama Cloud (local): set these in .env.local. Do NOT set VITE_OLLAMA_SERVER_ADDS_AUTH.
# VITE_OLLAMA_USE_CLOUD=true
# VITE_OLLAMA_API_KEY=your_ollama_cloud_api_key
# VITE_OLLAMA_MODEL=gpt-oss:20b-cloud

# Override API base URL (optional).
# VITE_OLLAMA_BASE_URL=

# --- Vercel (Project Settings → Environment Variables) ---
# Ollama Cloud via built-in proxy (key stays server-side):
#   VITE_OLLAMA_USE_CLOUD       = true
#   VITE_OLLAMA_SERVER_ADDS_AUTH = true
#   OLLAMA_API_KEY              = your_ollama_cloud_api_key   (server-only, no VITE_ prefix)
#   VITE_OLLAMA_MODEL           = gpt-oss:20b-cloud           (optional)
